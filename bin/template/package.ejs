{
	"name": "<%= packageName %>",
	"private": true,
	"version": "0.0.0",
	"scripts": {
		"dev": "vite",
		"build": "vue-tsc --noEmit && vite build",
		"preview": "vite preview"
		<% if (middleware.vitest) { %>,"test": "vitest"<% } %>
	},
	"dependencies": {
		"vue": "^3.2.37"
		<% if (middleware.vueRouter) { %>,"vue-router": "4"<% } %>
	},
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.3"
    ,"vite": "^3.0.7"
		<% if (middleware.vitest) { %>,"vitest": "^0.22.1"<% } %>
		<% if (middleware.scss) { %>,"sass": "^1.54.8"<% } %>
  }
}
